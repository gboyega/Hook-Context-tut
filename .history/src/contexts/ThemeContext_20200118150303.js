import React, { createContext } from 'react';

export const ThemeContext = createContext();

class ThemeContextP extends Component {
    state = {  }
    render() { 
        return (  );
    }
}
 
export default ThemeContextP;