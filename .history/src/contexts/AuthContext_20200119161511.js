import React, { Component, createContext } from 'react';

export const AuthContext = createContext();

class AuthContextProvi extends Component {
    state = {  }
    render() { 
        return (  );
    }
}
 
export default AuthContextProvi;
