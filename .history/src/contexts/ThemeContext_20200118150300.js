import React, { createContext } from 'react';

export const ThemeContext = createContext();

class ThemeContex extends Component {
    state = {  }
    render() { 
        return (  );
    }
}
 
export default ThemeContex;