import React, { Component, createContext } from 'react';

export const AuthContext = createContext();

class AuthContextP extends Component {
    state = {  }
    render() { 
        return (  );
    }
}
 
export default AuthContextP;
