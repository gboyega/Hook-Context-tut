import React, { createContext } from 'react';

export const ThemeContext = createContext();

class ThemeContextProvi extends Component {
    state = {  }
    render() { 
        return (  );
    }
}
 
export default ThemeContextProvi;